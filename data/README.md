# Data policy

This repo does not store large datasets.

- Small example files are in `data/small_examples/`.
- Most course data are fetched at runtime (PDB downloads, small ligand sets).
- Project data should be stored in each student's Google Drive project folder.

